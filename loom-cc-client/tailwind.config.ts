/**
 * Tailwind CSS v4 — config reference
 *
 * In Tailwind v4, theme tokens are defined in `src/app/globals.css` via `@theme { ... }`.
 * Content scanning is automatic — no `content` array needed.
 * This file exists for documentation and compatibility.
 *
 * Semantic color tokens (all defined in globals.css):
 *   accent-primary  #6366F1  靛蓝紫 — 主强调色、选中态、AI 功能
 *   accent-success  #32D583  翠绿  — 正确、成功
 *   accent-warning  #F59E0B  琥珀金 — 警告、提示
 *   accent-danger   #E85A4F  珊瑚红 — 错误、破坏性操作
 *
 *   bg-base         #0B0B0F  全局底色
 *   bg-nav          #0D0D12  左侧导航
 *   bg-surface      #141419  卡片、输入区
 *   bg-surface-high #1A1A22  激活高亮背景
 *   bg-input        #0F0F15  输入框背景
 *   border-subtle   rgba(255,255,255,0.07)  细边框、分割线
 *   border-strong   rgba(255,255,255,0.14)  强调边框
 *
 * Usage in components:
 *   className="bg-accent-primary text-bg-base"
 *   className="border border-border-subtle"
 */
export default {}
